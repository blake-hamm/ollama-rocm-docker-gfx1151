services:
  ollama:
    build: .
    ports: 
      - '0.0.0.0:11434:11434'
    volumes:
      - ~/.ollama:/root/.ollama
    devices:
      - /dev/kfd
      - /dev/dri
    security_opt:
      - seccomp=unconfined
